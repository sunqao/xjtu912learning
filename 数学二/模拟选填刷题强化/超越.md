## 2025超越一

### （1）函数有界 + $\lim_{x -> \infin} \frac{\int_0^x f(x) dx}{x}$的处理

函数有界就是看

（1）间断点是否有界

（2）定义域如果是开区间，看端点是否有界

对于其他的点是收敛点，收敛点一定有界

这种题目一般都是认为f(x)以T为周期，

x趋于正无穷，可以认为x：$nT \le x \le n(T + 1)$

当x卡在这个区间中的时候积分的范围是：
$$
n\int_0^T f(x) dx < \int_0^xf(x) dx < (n + 1)\int_0^Tf(x) dx
$$
所以可以使用夹逼定理：
$$
\frac{n\int_0^T f(x) dx}{n(T + 1)} < \frac{\int_0^xf(x) dx}{x} < \frac{(n + 1)\int_0^Tf(x) dx}{nT}
$$
x趋于正无穷，就等价于n趋于正无穷，这样可以得到极限：
$$
\lim_{x -> \infin} \frac{\int_0^x f(x) dx}{x} = \frac{\int_0^T f(x) dx}{T}
$$
这道题目在端点0处的处理就直接使用洛必达即可，可以得到x <= 2

在无穷的时候用上面的夹逼法则可以得到：
$$
\frac{n\int_0^T f(x) dx}{n^{\alpha}(T + 1)^{\alpha}} < \frac{\int_0^xf(x) dx}{x^{\alpha}} < \frac{(n + 1)\int_0^Tf(x) dx}{n^{\alpha}T^{\alpha}}
$$
要想趋于无穷的时候极限存在，那么$\alpha$必须大于等于1

### （2）$\ln(1 - e^{2x}) - 2x$的极限求法

这里先将2x放到ln中，然后再对x取正无穷

### （4）一些常见函数的导数要记住

主要是三角函数和反三角函数的导数

### （5）根据偏导数的定义来求分段点的偏导数

这个没啥好说的，就是根据偏导数的定义将$f_x’(x, y)$求出来，然后得到这个函数之后再求$f_{xx}''(x, y)$和$f_{xy}''(x, y)$

在分段点处必须得用定义求

在其他的地方可以用公式直接求导

### （7）周期函数的积分性质

**（1）周期函数在任意的周期长度的区间上的积分是一样的，只要积分区间的长度是T，结果就是一个定值**

**（2）$f(x)$是以T为周期的周期函数，那么$\int_0^xf(x)dx$为周期函数且周期是T的充要条件是$\int_0^T f(x) = 0$**

对于这种周期函数的积分，其性质的理解或者证明的常用方法就是对区间进行拆分

第一点

我们猜测其周期是T，那么有：
$$
F(x + T) = \int_{0}^{x + T}f(x) dx = \int_0^xf(x)dx + \int_x^{x + T}f(x)dx = \int_0^xf(x) dx + \int_0^Tf(x)dx
$$
显然最后一项为零那就是周期函数了

第二点

既然$\int_0^Tf(x)dx = 0$是变限函数是周期函数的的充要条件，那么就看看能否得到$\int_0^Tf(x)dx = 0$，由于f(x)是奇函数因此有：
$$
\int_{-\frac{T}{2}}^{\frac{T}{2}}f(x) dx = 0
$$
这样就得到了在一个积分区间是零，也就得到了条件

第三点

这一点和前面第一题的做法很像，对于周期函数的积分的做法，可以直接令x = nT，因此得到：
$$
\lim_{x -> \infin} \int_0^x f(x) dx=^{x = nT} = \int_0^{nT}f(x) dx = n\int_0^Tf(x) dx = 0
$$
同样得到了这个充要条件

第四点

还是用通用思路，对区间进行拆分，再用一次第一点性质就得到结果了，非常简单

### （8）正交矩阵的性质 + 矩阵乘法的行列式性质

**正交矩阵的两点性质：**

（1）每一行（列）的模值为1

（2）不同行（列）之间互相正交

（3）正交矩阵一定是可逆矩阵，并且$A^TA = AA^T = E$，因此正交矩阵的行列式的值一定是正负1

**矩阵乘法的行列式性质：**

$|AB| = |A||B|$

###  （9）特值法 + 广义初等变换 + 分块矩阵的秩的性质

这道题目可以令B = 0，那这样对于原矩阵来说其秩r = r(A) + r(C)

或者直接对原矩阵的第一列右乘$-A^{-1}B$加到第二列得到一个对角形式的矩阵，这样显然跟A的秩是相等的

对于B，第一行左乘-A加到第二行就跟A一样了

对于C，第一列右乘一个$-A^{-1}BC$加到第二列

### （13）已知一个变限积分求一个定积分 + 伽马函数$\int_0^{+\infin} e^{-x^2} dx$

关于这种变限积分与定积分的结合题目其实有两种做法：

（1）将其直接放进去当成二重积分来做

还是得用到伽马函数：
$$
\int_0^{+\infin} e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$
显然如果想要消掉1的话我们可以得到：
$$
\frac{2}{\sqrt{\pi}} \int_0^{+\infin} e^{-x^2} dx =  1
$$
这样可以变成：
$$
\int_0^{+\infin}[\frac{2}{\sqrt{\pi}} \int_0^{+\infin} e^{-t^2} dt - \frac{2}{\sqrt{\pi}} \int_0^{x} e^{-t^2} dt ]dx
$$
然后合并，交换积分次序即可

（2）直接用分部积分法可以消去一部分

这道题目可以直接带进去用分部积分来做，得到的项左边是一个趋于正无穷的极限，右边是正常的凑微分

### （14）高阶齐次线性微分方程的通解

## 2025超越二

### （1）间断点

只有在x0的去心邻域中有定义，这个点才算是f(x)的间断点

### （2）变限积分的奇偶性

第一个条件和第二个条件的判断直接拆开分别用变限积分奇偶性判断即可

第三第四个条件直接求导查看导数的正负

### （3）已知变限积分求定积分

这种题目的处理方式就是跟上文中差不多

（1）直接用分部积分做

（2）直接带进去看成二重积分，画出积分区域

这道题目直接将ux = t换元变成一个正常的变限积分之后代入到后面用二重积分做即可

### （4）带绝对值的积分函数的处理 + 拐点

对于$\int_0^1|x - t| dt$，注意对于这样的积分还是其实是可以写出表达式的，因为t的范围是[0, 1]，因此可以对x进行分类讨论，当x的范围在[0, 1]之间的时候可以将积分区域拆开得到表达式

得到表达式，根据题目中的微分方程来判断即可

这道题目二阶导都不是0，因此显然不是拐点，拐点是二阶导为零或者不存在的点

### （5）多元函数微分学的理解

这道题目，想要求u(1, 0)，那必须将u(x, y)的表达式得到，哪怕得不到也得得到一个大概的式子

怎么得到呢，只能从g(x, y)来入手

第一个条件是一个偏导有关的东西，那既然是偏导，我们试试对g(x, y) = u(x + y, x - y)求偏导试试，看能不能得到一些联系

接着就发现如果对y求偏导的话可以联系起来

然后求出g(x, y)的式子就好了，接着代入即可

这里需要注意的易错点是利用第一个条件代入第二条件时，第一个条件是u对第一个变量的偏导减去第二个变量的偏导得到一个关于第一个变量和第二个变量的函数，而不是单纯的x, y，在第二个条件求一次偏导使用第一条件的时候要将x + y看作一个整体

### （6）二重积分比大小

这道题，只要是二重积分，那必然就是先使用对称性

根据x的对称性以及轮换对称性得到被积函数：
$$
\iint (x^2 + y^2) \sin (x^2 + y^2) dxdy
$$
这里之所以出现比大小就是因为sin可能为负

对于I3来说其面积比前两个大，并且被积函数还是负的，当然就是最小了

### （9）非齐次线性方程的线性无关的解的个数 + AB = 0的性质

这道题目直接转换成齐次做就可以了

### （10）向量的线性组合形成的向量组的做法的通法

就是向量的线性组合形成的向量组的通法做法就是进行矩阵拆分（利用矩阵乘法的本质进行拆分）

### （12）参数方程的计算

这道题看似很复杂，其实需要转化成参数方程进行计算即可，注意参数方程的范围

### （13）零点处的高阶导处理方式

零点处的高阶导，首先看看函数的奇偶性，如果函数本身是奇函数，其求一次导是偶函数，再求一次导是奇函数，即偶次导是奇函数，那么显然偶次导那就是0了

因此这道题目就只剩下前面一项的处理了，根据莱布尼兹公式，其系数就只剩下$C_6^2 (x^2)^{(2)}(e^{2x})^{(4)}$这一项了，代入0得到 = 480了

### （14）对微分方程的理解

这里直接将这个微分方程y看作常数，这是一个关于x的一阶微分方程，直接套微分方程的公式法即可，但是注意这里的自由系数不是C了，而是一个关于y的函数f(y)